#pragma once

// Copy this file to config.h, then replace both values with your Wi-Fi
// credentials. main/config.h is ignored by Git.
#define CARDPUTER_WIFI_SSID "YOUR_WIFI_SSID"
#define CARDPUTER_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
